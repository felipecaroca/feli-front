export type WithOrganizationParam = {
  params: { 
    organization: string 
  }
}

export type WithModuleParam = {
  params: {
    moduleId: string
  }
}

export type WithAttentionParam = {
  params: {
    attentionId: string
  }
}
